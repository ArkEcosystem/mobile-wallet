export namespace AuthConfig {
	export const TOKEN = "auth";
	export const STORAGE_ATTEMPTS = "auth_attempts";
	export const STORAGE_UNLOCK_TIMESTAMP = "auth_unlock_timestamp";
	export const ATTEMPTS_LIMIT = 3;
	export const ATTEMPTS_TIMEOUT_SECONDS = 30;
}
