export namespace AuthConfig {
	export const TOKEN = "auth";
	export const STORAGE_SESSION_ID = "auth_session_id";
	export const STORAGE_EXPIRES_AT = "auth_expires_at";
	export const EXPIRES_IN_SECONDS = 120;
}
